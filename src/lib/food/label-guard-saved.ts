/** 與 DB `enforce_label_guard_saved_reports_limit` 一致 */
export const MAX_LABEL_GUARD_SAVED_REPORTS = 5;

/** 與 DB `label_guard_saved_reports.name` CHECK 一致 */
export const MAX_LABEL_GUARD_SAVED_NAME_LENGTH = 30;
