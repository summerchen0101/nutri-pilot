---
description: AI 任務分類、Queue 架構、Prompt 設計
globs: "**/ai/**,**/functions/ai*,**/*claude*,**/*queue*"
alwaysApply: false
---

# AI 引擎規則

## 任務分類（最重要）

必須走 Queue（QStash）：

- 拍照辨識餐桌（圖片上傳 + Vision，`ai-photo-analyze` → `photo_analysis_jobs`）
- 食品標示智慧分析／守衛（獨立 bucket，`label-guard-analyze` → `label_guard_jobs`）
- 週報洞察（cron 觸發）

直接呼叫（可以等）：

- 換食材建議
- 今日 Dashboard 建議

## Queue 流程

餐桌拍照：前端上傳 `food-photos` / 建立 `photo_analysis_jobs` → QStash → `ai-photo-analyze`
→ 寫回 `photo_analysis_jobs`。

守衛標示：前端上傳 `label-guard-photos` / 建立 `label_guard_jobs` → QStash → `label-guard-analyze`
→ 寫回 `label_guard_jobs`。前端均以 polling / Realtime 更新。

週報洞察：pg_cron 觸發 `ai-weekly-insight`
→ Worker 聚合近 7 天資料 → 呼叫 Claude → 寫入 `weekly_insights`

## Claude wrapper

用 `lib/ai/claude.ts` 的 `callClaudeJSON<T>()` 函數。
模型固定用 `claude-sonnet-4-5`。
JSON 任務在 prompt 最後加：「只回傳 JSON，不加 markdown 或說明文字。」

## Prompt 函數位置

`lib/ai/prompts/` 資料夾，每個任務一個檔案。
不要把 prompt 字串直接寫在元件或 Edge Function 裡。

完整 Prompt 設計見 `/docs/04-ai-engine.md`。
