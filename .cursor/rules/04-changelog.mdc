---
description: 開發過程中與規格書有差異時記錄異動（簡版、省 token）
alwaysApply: true
---

# 規格異動記錄規則

記錄目的為 **追溯規格／實作落差**；撰寫與對話輸出以 **簡潔為原則**，避免長篇複製規格原文或重述整個實作（節省 token 與審閱時間）。

## 觸發條件（需記錄）

僅當 **實作已與 `/docs/` 內對應規格不一致**（或規格未定義但你做了會影響產品行為／契約的決策）時建立檔案，例如：

- 規格未提及但你實作的功能、欄位、流程
- 規格已寫但做法改變
- 技術限制導致換方案
- Schema／API／對外契約異動（含 Edge Function、金流錯誤語意）
- 臨時決策／先上線後補規格之情況

## 不需記錄

- **與規格一致**之重構、換名、調整版面細節、純視覺、純 linter／型別修補（除非同時改了契約）

## 合併與檔名

- **同日／同一議題**：可合在 **一篇** `[日期]-[主題].md`，以項目符號列出多個異動。
- **不同議題**：維持一題一檔：`/docs/changes/YYYY-MM-DD-[簡短描述].md`（例如 `2026-04-18-schema-add-photo-jobs.md`）。

## 簡版格式（預設，強制優先）

單檔 **總量約一螢幕內**。不要貼規格書大段原文；改用 **`docs/` 路徑與章節錨點** 指路。類型標籤（新增／修改／刪除／技術替換／臨時決策）可併入標題或「異動摘要」第一點，不必獨立欄位。

必填段落與順序：

1. 標題行：`# [一行標題]`
2. **日期**：`YYYY-MM-DD`
3. **影響規格**：列出影響的 `docs/…`（可複數），或可寫「無；僅 changelog」
4. **異動摘要**：項目符號 2～6 點——做了什麼、與規格差異（每點一句為主）
5. **原因／後續**：各一兩句——為何如此改／是否要回頭更新規格書、是否視為永久方案

## 完整格式（僅在必須釐清合約或稽核時）

涉法規、金流、權限或需正式對外文時，可用舊有結構區分：**原規格**、**實際做法**、**原因**、**後續**（可保留 **類型** 欄若需要）；仍避免全文引用規格，以摘要＋連結為主。

## 操作方式（AI／人類）

1. **有異動且符合觸發條件**：在 `/docs/changes/` 新增或更新對應 `.md`，**一律先採簡版**。
2. **對話中**：不要將整份異動檔再貼一次；一句話指路徑即可，例如：`已記錄異動：docs/changes/YYYY-MM-xx-….md`
3. **無異動**：回應結尾註記 `✓ 無規格異動`。
4. 若偏好 **人手維護 changelog**：可自行新增／修改檔案並在 PR／commit 說明附路徑；AI 以對話確認已有人類記錄或可跳過自動建檔（依團隊習慣）。
