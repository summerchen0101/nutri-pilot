---
description: UI 設計系統，所有元件的色彩、字體、間距、圓角規範
globs: "**/*.tsx,**/*.css,**/*.module.css,**/components/**,**/app/**"
alwaysApply: false
---

# UI 設計規範

完整規格見 `/docs/09-ui-design.md`，以下是高頻使用的快速參考。

## 色彩

主色 Sea Green：`#4C956C`（互動、outline、進度／checkbox／nav active 文字）、`#3A7A56`（加深）、`#E8F5EE`（淡背景）、`#2D6B4A`（淡底上的文字）  
Primary CTA（Shadow Grey）：`#1E212B`／hover `#2A2F3D` — **僅**主要操作按鈕（`Button` default）  
輔色 Amber：`#EF9F27`（脂肪進度、警示）  
第三色 Blue：`#378ADD`（碳水進度）、`#E6F1FB`（AI 建議背景）  
危險色：`#E55A3C`（刪除按鈕）、`#E24B4A`（錯誤文字）

中性色全用 CSS 變數（`--color-text-primary` 等），自動 dark mode。
**不硬寫 `#000`、`#fff`、`#333` 等純黑純白。**

## 字體

**語意 class（優先）**：`text-heading-screen`（螢幕頂主標／`PageHeading`）、`text-heading-page`（數字大值；**Bottom Sheet／Portal 對話框標題**亦用此級）、`text-heading-section`（區塊）、`text-heading-card`（卡片小標）、`text-body`（內文）、`text-caption`（輔助）、`text-micro`（nav／角標）。對應元件：`PageHeading`、`SectionHeading`、`CardTitle`／`CardDescription` 已內建。

px 階層（與 `tailwind.config.ts` 對齊）：

```
螢幕頂主標  24px / 500  → text-heading-screen（PageHeading、登入／Onboarding 頂標）
數字大值    20px / 500  → text-heading-page（sheet／dialog 標題同級）
區塊標題    15px / 500  → text-heading-section
卡片小標    13px / 500  → text-heading-card
內文        13px / 400  → text-body（字重繼承 body）
輔助文字    11px / 400  → text-caption
nav 標籤    10px / 400  → text-micro
數字大值單位              text-body
最粗只到 font-weight: 500，不用 600 / 700
```

**PageHeader**：僅主標與可選 `meta`，無頁面說明副文。

**禁止**：未在規格表的任意字級（例如 `12px`、`text-xs`／`text-sm` 代替上列語意）；新稿請改為 `text-caption` 或 `text-body`。

**按鈕**：使用 `Button` 元件；`size="default"`（13px、`min-height` 44px、padding 11px 18px、圓角 10px）、`size="sm"`（11px、`min-height` 40px、小按鈕）。

## 邊框

極細線：**`pointer: fine` → 0.5px；`pointer: coarse`（多數手機）→ 1px**，由 `tailwind-hairline-plugin.ts`（`border-hairline`、`border-*-hairline`、`divide-y-hairline`）處理，避免 WebKit 左側線不畫。Recharts 等 inline border 用 `var(--hairline-border-shorthand)`（coarse 寬度同由該 plugin `addBase`）。不在元件散落任意 `1px`。
```css
卡片預設：  border: 0.5px solid var(--color-border-tertiary)（觸控裝置實際 1px 由全域規則）
hover：     border-color: var(--color-border-secondary)
主色強調：  border: 1.5px solid #4C956C
input focus：border-color: #4C956C; box-shadow: 0 0 0 2px rgba(76,149,108,.12)
```

## 圓角

```
badge / tag     border-radius: 9999px
小按鈕          border-radius: 8px
按鈕 / 輸入框   border-radius: 10px
卡片            border-radius: 12px（最常用）
modal / sheet   border-radius: 16px
```

## 常用元件 Tailwind class 對照

```
主按鈕：    使用 <Button variant="default" size="default" />（實作為 shadow-grey、text-body font-medium、px-[18px] py-[11px]、min-h-11）
小按鈕：    <Button size="sm" />
次要按鈕：  <Button variant="outline" />
卡片：      bg-background border-hairline border-border rounded-xl p-4
成功 pill： bg-[#E8F5EE] text-[#2D6B4A] rounded-full px-2.5 py-0.5 text-caption font-medium
bottom nav active： bg-[#E8F5EE] text-[#4C956C]
AI 卡：     bg-[#E6F1FB] border-hairline border-[#B5D4F4] rounded-xl p-3.5
指標格：    bg-secondary rounded-[10px] p-3 cursor-pointer
bottom sheet 標題： text-heading-page（`BottomSheetShell`）
```

## 禁止

- 不用漸層背景
- 不用 box-shadow 做層次
- 不用 font-weight 超過 500
- 不用低於 10px 的字
- 商城頁不做即時熱量對比，只做長期飲食計畫符合度說明
